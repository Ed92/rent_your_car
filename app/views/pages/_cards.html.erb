
<div class="wrapper">
  <div class="container">
    <div class="cards">
        <% @cars.each do |c| %>
          <span class="card"><%= render 'pages/card', car: c %></span>
        <% end %>
    </div>
  </div>
</div>
